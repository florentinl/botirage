name: "Build package"
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: [self-hosted, linux, ARM64]
    steps:
        - uses: actions/checkout@v4
        - run: |
            nix build
